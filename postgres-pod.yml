apiVersion: v1
kind: Pod
metadata:
  name: postgres-app
  labels:
    name: postgres-pod
    app: voting-app
spec:
  containers:
    - name: postgres
      image: postgres:9.4
      ports:
        - containerPort: 5432